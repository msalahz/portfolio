//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  semi: false,
  printWidth: 100,
  singleQuote: true,
  trailingComma: 'es5',
  plugins: ['prettier-plugin-tailwindcss'],
}

export default config
